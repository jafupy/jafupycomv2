---
interface Props {
	class?: string;
	noMain?: boolean;
}

const { class: className, noMain } = Astro.props;

import Root from "./root.astro";
---

<Root class={className}>
	<article class="max-w-ch-md">
		{
			noMain ? (
				<slot />
			) : (
				<main>
					<slot />
				</main>
			)
		}
	</article>
</Root>
